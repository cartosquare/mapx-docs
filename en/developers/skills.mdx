---
title: "Skills"
description: "Guidance documents that teach AI agents how to work with MapX."
---

MapX ships a set of Markdown guidance documents for AI agents. CLI users can
load them on demand:

CLI users can load them on demand:

```bash
mapx skill
```

The current skill index is stored in `source/skills.json`.
